[project]
name = "langchain-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "karatejb", email = "ppippenkimo@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.4.0",
    "langchain-google-genai>=4.4.0",
]

[project.scripts]
langchain-playground = "langchain_playground:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
